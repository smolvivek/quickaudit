// Theme.js - QuickAudit App Theme System
// Combines Colors, Typography, and Spacing into a unified theme

import COLORS from './Colors';
import { TYPOGRAPHY, TEXT_STYLES } from './Typography';
import SPACING from './Spacing';

// Create a unified theme object
const Theme = {
  COLORS,
  TYPOGRAPHY,
  TEXT_STYLES,
  SPACING,
};

export default Theme;
